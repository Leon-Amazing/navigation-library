import { defineConfig } from 'dumi';

export default defineConfig({
  base: '/navigation-library',
  publicPath: '/navigation-library/',
  favicons: ['/navigation-library/favicon.ico'],
  themeConfig: {
    logo: false,
    footer: false,
  },
});
